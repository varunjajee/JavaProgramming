package com.muks.DSC.Random;

public class Dictionary {
	/*
	 * NOTE:
	 * 
	 * Collision: in hash tables when two keys come at the same location is called collision
	 * How to deal with 2 keys mapping to the same location of an array => using "Chaining"
	 * 
	 *  Chaining:
	 *  Mapping the keys to a hash key with a linked list, adding keys to a linked list which is mapped to a location of a hash
	 *  Chaining is using linked list to store keys
	 *  
	 * 
	 */
}
